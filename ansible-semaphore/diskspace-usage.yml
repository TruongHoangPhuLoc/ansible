- hosts: all
  user: locthp
  become: true
  tasks:
  - name: Check diskspace usage of host
    shell: df -h | awk 'NR > 1 {print $6 "\t" $5}'
    when: ansible_distribution == "Ubuntu"
    register: result
  - name: Process disk usage data
    ansible.builtin.set_fact:
       processed_output: "{{ result.stdout_lines | map('split', '\t') | list }}"
  - name: Print the output of result
     debug:
          #Process list of fs' usage here
        msg: "{{ processed_output }}"
  #   with_items:
  #        - "{{ result.stdout_lines }}"
  #   where: item