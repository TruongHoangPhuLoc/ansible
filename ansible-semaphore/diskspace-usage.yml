- hosts: all
  user: locthp
  become: true
  tasks:
  - name: Check diskspace usage of host
    shell: df -h | awk 'NR > 1 {print $5}'
    when: ansible_distribution == "Ubuntu"
  register: result
  
  - name: Print the output of result
    debug:
      msg: "{{ result.stdout }}"